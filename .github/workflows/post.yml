name: Post Exam Prep to Telegram Channel

on:
  push:
    branches:
      - main
    paths-ignore:
      - topic_index.txt
  workflow_dispatch: {}
  schedule:
    - cron: "30 2 * * *" # 02:30 UTC -> 08:00 IST
    - cron: "30 5 * * *" # 05:30 UTC -> 11:00 IST
    - cron: "30 8 * * *" # 08:30 UTC -> 14:00 IST
    - cron: "30 11 * * *" # 11:30 UTC -> 17:00 IST
    - cron: "30 16 * * *" # 16:30 UTC -> 22:00 IST

permissions:
  contents: write

jobs:
  post:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: python -m pip install --upgrade pip && pip install -r requirements.txt

      - name: Run bot once
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN || vars.TELEGRAM_BOT_TOKEN }}
          OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY || vars.OPENROUTER_API_KEY }}
          TELEGRAM_CHANNEL_ID: ${{ secrets.TELEGRAM_CHANNEL_ID || vars.TELEGRAM_CHANNEL_ID }}
        run: python bot.py --once

      - name: Commit updated index if changed
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          if ! git diff --quiet -- topic_index.txt; then
            git add topic_index.txt
            git commit -m "Update topic_index after scheduled post"
            git push
          else
            echo "No topic_index changes to commit."
          fi
